<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    home
    <a href="/form">Form</a>
<div> <a href="/">logout</a> </div>
    <% data.map((el, i) => { %>
        <tr>
           
            <td><%= el.name %></td>
            <td><%= el.email %></td>
            <td><%= el.password %></td>
          
            <td>
                <a href="/deleteData?id=<%= el.id %>"><button class="btn btn-delete">Delete</button></a>
                <a href="/editData?id=<%= el.id %>"><button class="btn btn-edit">Edit</button></a>
            </td>
            
        </tr>
    <% }) %>
</body>

</html>